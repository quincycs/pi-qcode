{
  "name": "qcode",
  "displayName": "QCode",
  "description": "A bare minimal VS Code extension home screen for recent Pi sessions.",
  "version": "0.0.1",
  "publisher": "local",
  "license": "UNLICENSED",
  "engines": {
    "vscode": "^1.92.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:qcode.home"
  ],
  "main": "./src/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "qcode",
          "title": "QCode",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "qcode": [
        {
          "type": "webview",
          "id": "qcode.home",
          "name": "Home"
        }
      ]
    }
  },
  "scripts": {
    "package-install": "vsce package --allow-missing-repository --out qcode-0.0.1.vsix && code --install-extension qcode-0.0.1.vsix --force"
  },
  "devDependencies": {
    "@vscode/vsce": "^3.6.0"
  }
}