when messages are sent via the socket over to the pi agent, the agent doesn't write the sent message into the session. I'd like the session file to show this message as "user" message. Is there changes to pi-msg possible to allow this?

Don't change code to ../pi-msg , instead just investigate.
