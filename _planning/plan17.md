update the settings ui so that the value of the command is a multiline text field.

I'd like initially the commands to be listed with a pencil icon next to them. clicking the pencil should transform the item from a title / subtitle view (where the command is the title and the value is the subtitle) into two text fields (one for the command on one line, and another for the value on the next line that supports multi line input). On the 3rd line, I'd like a save button, cancel button, and a delete button. The delete button should prompt a confirmation before performing the delete action.
