the below doesn't render nicely in the UI (session detail). Look to why that's the case. i think it's because of the code blocks have spaces beforehand. I think the parsing doesn't handle that well.

---

apples have oranges, and bananas.

Flow:

1. First apples eat oranges, then bananas.

   ```ts
   getStuff(appleId);
   // GET /apple-api/stuff
   ```

   stuff get's called and fruits and vegs they all get into a car and speak to their landlord , asking what they can do.

2. When the vegs are called:

   ```ts
   getVegs(selectedAppleId);
   // GET /apple-api/vegs
   ```
