create a bare minimal vscode extension taking inspiration from
/Users/qmitchell/proj/PiDE_Piper_VSCode_Extention

i'd like the installation steps to be:

npm install
npm run package-install

I'd like the extension UI only contain a simple "hello world" text and nothing more.  focus on
the bare minimal extension to make it achieve this.  don't include unnecessary code further than that.
