if any of the messages are right clicked , then I'd like a context menu to appear with an option to "copy message" which would copy the whole raw message to the clipboard.

I'm okay with the default context menu being overridden but for highlighted text the menu item should be "copy".

The default context menu doesn't make a lot of sense b/c "cut" and "paste" don't do anything. But the "copy" is useful.
