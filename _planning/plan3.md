Render the session detail screen with an existing session.

1. update the home screen list items to be clickable. On click, navigate to the session detail screen with the session filepath as a parameter. In future iterations, a 'new session' button will be added to the home screen which will make the filepath be optional.

2. Navigate from the home screen to a session detail screen. Allow a user to click a home button to return to the home screen.

3. On the session detail screen, if the filepath is provided, then read the session file and simply display the number of lines in the file. Let the main body of the screen be scrollable except for a header (which contains the home button) and a footer which will contain a text input and a submit icon button. The submit button should do nothing for now. A future task will further define this.
