I'm finding that the rendering of numbered lists is more problematic than benefitual. I'd rather them to render raw instead.

I do like the bullet point rendering, so let's keep that. But for numbered lists, simplify the code and just render the raw.
