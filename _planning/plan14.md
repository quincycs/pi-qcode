I'd like the vscode extension to integrate with the vscode context menu so that code can be highlighted , right clicked and select "Add to Qcode". This should then add the highlighted code into the text input of the session detail view.

It should purely add text to the end of the input, it should not send the message.
