I'd like the textinput on sessiondetail to have a typeahead/autocomplete feature for mentioning files. "@" should trigger the typeahead, and then the user can start typing a filename. The typeahead should show a list of files that match the input, and the user can select one or press tab to insert it into the textinput.

look at <redacted> for inspiration. it has a vscode extension that provides a similar feature.
