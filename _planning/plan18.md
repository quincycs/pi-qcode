on the session detail screen, remove the title of the screen and replace it with a small progress bar that when hovered describes the % of the context window used.

Look at ../PiDE_Piper_VSCode_Extention for inspriation on how to detect the context window and how to calculate the % of the context window used.

i think the assistent messages contain a usage field. look at the Pi session format for reference.
